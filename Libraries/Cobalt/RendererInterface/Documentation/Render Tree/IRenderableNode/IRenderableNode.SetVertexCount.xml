<?xml version="1.0" encoding="utf-8" ?>
<XMLDocContent PageName="cobalt.graphics.IRenderableNode.SetVertexCount" Title="SetVertexCount method" xmlns="http://www.cobaltrenderer.com/schema/XMLDocSchema.xsd">
  <Paragraph>
    <PageRef PageName="cobalt.graphics.Welcome">Home</PageRef> ->
    <PageRef PageName="cobalt.graphics.IRenderableNode">IRenderableNode</PageRef> ->
    SetVertexCount
  </Paragraph>
  <Section Title="Description">
    <Paragraph>
      The <TypeRef>SetVertexCount</TypeRef> method is used to update the the number of vertices that are drawn for this
      <TypeRef>IRenderableNode</TypeRef>, as well as specifying offsets for the vertex and index buffers. This allows
      any one sub-range of the bound index and vertex buffers to be used by this renderable during the draw process.
    </Paragraph>
  </Section>
  <Note Type="Important">
    <Paragraph>
      For the simple case where an <TypeRef>IRenderableNode</TypeRef> needs to draw the entire contents of the bound
      vertex and index buffers, this method doesn't need to be called at all. By default, the <TypeRef>IRenderableNode</TypeRef>
      will use the total number of indices in the bound index buffer, or the total vertex count in the last bound vertex
      attribute to determine the vertex count, and all offsets will be set to 0. If this method is called once however,
      the vertex count and offsets will be fixed to the manually specified values.
    </Paragraph>
  </Note>
  <Note Type="Important">
    <Paragraph>
      This method can be called at any time after the creation of the <TypeRef>IRenderableNode</TypeRef>, including when
      it has already been added to the render tree. Any changes made will not affect any current frame being drawn, and
      will take effect on the next frame.
    </Paragraph>
  </Note>
  <Section Title="Usage">
    <Code Language="C++"><![CDATA[SuccessToken SetVertexCount(size_t vertexCount, size_t vertexBufferOffset = 0, size_t indexBufferOffset = 0, ptrdiff_t indexValueOffset = 0);]]></Code>
    <SubSection Title="Argument list">
      <ArgumentList>
        <ArgumentListEntry Type="size_t" Name="vertexCount">
          Specifies the number of vertices which will be drawn for this renderable
        </ArgumentListEntry>
        <ArgumentListEntry Type="size_t" Name="vertexBufferOffset">
          Specifies an initial offset into each vertex attribute buffer, in units of whole vertices. For example, an
          offset of 10 will cause the first vertex to be read from the 10th position in each vertex attribute array.
        </ArgumentListEntry>
        <ArgumentListEntry Type="size_t" Name="indexBufferOffset">
          Specifies an initial offset into the index attribute buffer, in units of whole indices. For example, an offset
          of 10 will cause the first index to be read from the 10th position in the index attribute array.
        </ArgumentListEntry>
        <ArgumentListEntry Type="ptrdiff_t" Name="indexValueOffset">
          Specifies an offset to be applied to each index value read from the index array. This is a signed value, so
          the offset applied can either be positive or negative.
        </ArgumentListEntry>
      </ArgumentList>
    </SubSection>
    <SubSection Title="Return value">
      <ReturnValue Type="SuccessToken!cobalt.graphics.SuccessToken">
        True if the vertex and index count and offset values were successfully updated, false otherwise. There are
        currently no circumstances under which this method will fail, however in future updates the supplied values may
        be validated in debug builds to ensure they meet constraints set by the defined <PageRef PageName="cobalt.graphics.IRenderableNode.PrimitiveMode">PrimitiveMode</PageRef>,
        and the types and sizes of the bound index and vertex buffers. An assertion will be raised in debug builds if
        the result isn't tested.
      </ReturnValue>
    </SubSection>
  </Section>
  <Section Title="See also">
    <PageRefList>
      <PageRefListEntry PageName="cobalt.graphics.IRenderableNode">IRenderableNode</PageRefListEntry>
    </PageRefList>
  </Section>
</XMLDocContent>
