<?xml version="1.0" encoding="utf-8" ?>
<XMLDocContent PageName="cobalt.graphics.IRenderer.Delete" Title="Delete method" xmlns="http://www.cobaltrenderer.com/schema/XMLDocSchema.xsd">
  <Paragraph>
    <PageRef PageName="cobalt.graphics.Welcome">Home</PageRef> ->
    <PageRef PageName="cobalt.graphics.IRenderer">IRenderer</PageRef> ->
    Delete
  </Paragraph>
  <Section Title="Description">
    <Paragraph>
      The <TypeRef>Delete</TypeRef> method destroys this renderer. Note that unlike all created objects from the <TypeRef>IRenderer</TypeRef>
      interface, deleting the renderer does not perform deferred destruction, and fully destroys the object before it
      returns. It is the responsibility of the application to ensure that all objects created through the <TypeRef>IRenderer</TypeRef>
      interface are destroyed prior to destroying the renderer itself. Allocated objects alive after the destruction of
      the renderer may lead to runtime failures.
    </Paragraph>
    <Note Type="Warning">
      <Paragraph>
        Under normal circumstances, this method should never need to be called directly. The <TypeRef>unique_ptr</TypeRef>
        which is returned when this object is constructed will call this method itself when the pointer is released. You
        must not call this method if the object lifetime is still being managed by the <TypeRef>unique_ptr</TypeRef>
        object, otherwise the method will be called more than once. If this occurs the behaviour is undefined, and it
        may result in runtime errors.
      </Paragraph>
    </Note>
  </Section>
  <Section Title="Usage">
    <Code Language="C++"><![CDATA[void Delete();]]></Code>
  </Section>
  <Section Title="See also">
    <PageRefList>
      <PageRefListEntry PageName="cobalt.graphics.IRenderer">IRenderer</PageRefListEntry>
    </PageRefList>
  </Section>
</XMLDocContent>
